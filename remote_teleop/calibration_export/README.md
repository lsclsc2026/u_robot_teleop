# 历史从臂标定导出

`aloha_follower.json` 仅对应原串口序列号 **5B3D047726** 的 SO-101 从臂，是历史 PC1 导出；不是当前下位机标定有效性的保证，也不是通用参数。恢复前应确认同一实际设备、关节、电机 ID、零点和 EEPROM 状态。原 [README.txt](README.txt) 的复制目标只说明历史部署路径，不能理解为无条件覆盖指令。

主臂设备为另一序列号 **5B3D047743**，其备份在仓库根目录 `calibration/`，不能混用。详见[设备适用说明](../../calibration/README.md)。
